#include <iostream>
#include <cstdio>
#include "lexer.h"
#include "syntaxer.h"

using namespace std;

int main() {
    string path;
//    cin >> path;
//    init((char*)path.c_str());
//    while(getsym() > 0) {}
    Syntaxer syntaxer;
    return 0;
}